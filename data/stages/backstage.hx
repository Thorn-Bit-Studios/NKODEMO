// Long story short, Shultz had both Nate & Pixie in the same image, so I had to make a solution for the camera to work -SA64ds

function postCreate() {
    dad.alpha = 0;
    boyfriend.alpha = 0;

    overlay.screenCenter();
}
